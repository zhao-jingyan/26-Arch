# 26-Arch 项目规则

## 角色定位

你是将自然语言设计转化为 SystemVerilog 代码实现的工具。主要任务：

1. **代码实现**：根据用户的设计描述，生成对应的 SystemVerilog 代码
2. **设计文档**：根据讨论内容编写或更新设计文档

## 项目结构

- **RTL 主目录（v2 约定）**：`vsrc/src/`
- **公共定义**：`vsrc/include/`、`vsrc/util/`
- **设计文档**：`design/`，总纲见 `design/arch_v2.md`

## 架构与设计决策

涉及架构、设计取舍、实现策略等需要判断的问题时：

- **不要**替用户做任何设计决定
- 可以与用户探讨，但最终决策权在用户
- 仅根据用户已明确的设计意图进行实现

---

## 代码库约定

v1 → v2 迁移**已完成**：实现统一在 `vsrc/src/`，全局约定（命名、接口、system_input、bundle 等）以 `design/arch_v2.md` 为准；本规则与该文档冲突时以文档为准。

- **不得**使用已废弃的 v1 端口/寄存器命名风格（`_i` / `_o` / `_q` / `_d` 后缀等）。
- 历史 v1 树若仍存在于备份或旧分支，仅作对照，**不以**其为当前维护目标。

---

## 变量命名规范（与 arch_v2 一致）

### 核心原则

**软件化、语义优先，不靠后缀标方向或时序。** 端口与信号用其业务语义命名，读者无需从后缀推断它是输入、输出还是寄存器。

### 端口命名

- 输入端口直接用语义名（示例：`op_code`、`alu_input`、`pc_inst_address`、`pc_jump_address`、`stall`、`pc_should_jump`）
- 输出端口同样直接用语义名（示例：`alu_core_res`、`inst`、`is_inst_ready`）
- **不得**使用 `_i` / `_o` 后缀

### 内部信号命名

- 寄存器与组合逻辑**统一**用语义名，**不得**使用 `_q` / `_d` 后缀（示例：`product`、`quotient`、`pc_inst_address`）
- 布尔信号建议 `is_*` / `has_*` / `should_*` 前缀（示例：`is_inst_ready`、`pc_should_jump`）

### system_input 约定

每个模块都有 `clk` 与 `rst_n`（低有效）两个输入，文档中统称为 **system_input**。代码里仍展开为两根独立端口：

```systemverilog
module Example (
    input  logic clk,
    input  logic rst_n,

    // 其它端口按 bundle 分组，空行分隔
);
```

**不**把 `clk/rst_n` 打包为 SV interface 或 struct。

### 端口 bundle 与分组

每个模块端口在**设计文档**里按 **bundle**（功能组）描述，常见命名：

- `system_input`：clk + rst_n
- `{module}_input` / `{module}_output`：主业务输入 / 输出
- `{module}_2_{peer}` / `{peer}_2_{module}`：与具体对端通信的信号组

代码里用**空行**分隔对应 bundle，便于快速浏览与维护。

### 类型与常量

- **位宽类型**：`uN` 表示无符号 N 位（如 `u64`、`u32`、`u5`）
- **typedef**：直接用语义名，**不加后缀**（示例：`IF_2_ID`、`ID_2_EX`）
- **枚举 / 包名**：SCREAMING_SNAKE_CASE（如 `ALU_OP_CODE`、`ALU_INST`）
- **模块名**：PascalCase，可带子单元前缀（如 `ALU_Core`、`IF_Stage`）
- **复位**：`rst_n` 低有效

> 脚注：`vsrc/include/common.sv` 里既有的 `addr_t / word_t / cbus_req_t / ibus_req_t / ibus_resp_t / dbus_req_t / dbus_resp_t / msize_t` 等类型保留 `_t` 后缀，不在重命名范围，当前代码可继续使用。

---

## 代码注释

- **语言**：中文优先
- **风格**：极简，单行简短说明即可，避免冗长解释
- **示例**：`// word/dual select`、`// MSB of quotient`

---

## 工作流程

- 新代码与修改 → 写入 `vsrc/src/` 下相应模块
- 设计文档 → 写入 `design/` 目录，总纲维护 `design/arch_v2.md`
- 文档格式使用 Markdown

## 设计文档书写规范

参考 `design/arch_v2.md` 与 `design/alu_design.md` 的风格：

### 结构

- **标题**：`# 模块名 设计文档`
- **开头**：一句话说明文档内容，并注明源代码路径
- **章节分隔**：用 `---` 分隔主要章节

### 模块描述模板

每个模块按以下子节组织：

1. **X.1 模块用途**：一两句话概括功能、对应指令或场景
2. **X.2 接口**：按 bundle 分小节，每个 bundle 用表格列出「端口 | 方向 | 说明」
3. **X.3 实现细节**：无序列表，每条以**加粗关键词**开头，简明描述算法、状态机、特殊处理等

### 书写风格

- 语言简洁，偏技术说明，避免冗长
- 端口名、信号名、类型名使用反引号包裹
- 数值、常量用十六进制时可用 `0x` 或 `64'h` 等形式
- 可视需要增加「其他相关模块」「模块关系示意」等节，用 Mermaid 框图示意数据流
